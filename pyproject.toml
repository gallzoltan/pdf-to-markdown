[project]
name = "pdf-marker"
version = "0.1.0"
description = "pdf to markdown"
readme = "README.md"
requires-python = ">=3.12.11"
dependencies = [
    "marker-pdf>=1.10.2",
    "python-dotenv>=1.2.2",
]
